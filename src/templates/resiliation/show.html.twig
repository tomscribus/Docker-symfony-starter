{% extends 'base.html.twig' %}

{% block title %}Resiliation{% endblock %}

{% block body %}
    <h1>Resiliation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ resiliation.id }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ resiliation.status }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('resiliation_index') }}">back to list</a>

    <a href="{{ path('resiliation_edit', {'id': resiliation.id}) }}">edit</a>

    {{ include('resiliation/_delete_form.html.twig') }}
{% endblock %}
