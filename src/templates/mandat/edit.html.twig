{% extends 'base.html.twig' %}

{% block title %}Editer le Mandat{% endblock %}

{% block body %}
    <h1>Editer le Mandat</h1>

    {{ include('mandat/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

    <a class="btn btn-primary btn-user" href="{{ path('mandat_index') }}"> &#8592; Retour à la liste des mandats</a>

    {{ include('mandat/_delete_form.html.twig') }}
{% endblock %}
